server:
  port: 8004
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8003/eureka
spring:
  application:
    name: feign8004

